import { forwardRef } from "react";

const ProjectsSection = forwardRef((props, ref) => {
  return (
    <div ref={ref} className="container mx-auto h-screen bg-yellow-200">
      ProjectsSection
    </div>
  );
});

export default ProjectsSection;
